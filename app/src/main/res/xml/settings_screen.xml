<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">


    <CheckBoxPreference
        android:defaultValue="false"
        android:key="sent_only_to_rel_checkbox"
        android:maxLines="1"
        android:summary="Enabling this messages will be sent only to those users whom you have set as Reliable"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:title="Broadcast to Reliable Users only"
        />

    <SeekBarPreference
        android:id="@+id/radius_slider"
        android:key="radius_slider"
        android:summary = "GeoCast broadcasts your message within a geo radius"
        android:max="100"
        android:title="Set Radius"
        />




</PreferenceScreen>
